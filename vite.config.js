// Vite config for GitHub Pages deployment
import { defineConfig } from 'vite';

export default defineConfig({
  // Set the base path to your repo name for correct asset loading on GitHub Pages
  base: '/Runecraft/',
});
