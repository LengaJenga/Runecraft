{
  "name": "runecraft",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^2.6.4",
    "gh-pages": "^3.2.3"
  },
  "dependencies": {
    "three": "^0.133.1"
  }
}
